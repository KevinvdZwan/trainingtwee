{% extends 'base.html.twig' %}

{% block title %}Edit Training{% endblock %}

{% block body %}
    <h1>Edit Training</h1>

    {{ include('training/form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('training_index') }}">back to list</a>

    {{ include('training/delete_form.html.twig') }}
{% endblock %}
